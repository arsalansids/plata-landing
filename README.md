# Run the static landing page

Make sure you have the jekyll gem installed first, had to use RVM with ruby version 3.1.3 to make current jekyll work. 
```
gem install jekyll bundler
```

```
jekyll serve
```

